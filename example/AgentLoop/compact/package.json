{
  "name": "agent-context-compact",
  "version": "0.1.0",
  "description": "Agent Loop context compaction module — summarize historical messages to reduce token usage",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:unit": "vitest run tests/core",
    "test:integration": "vitest run tests/infrastructure",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "dep-check": "depcruise src --config .dependency-cruiser.cjs",
    "check": "npm run lint && npm run dep-check && npm run test:coverage && npm run build",
    "example:basic": "tsx examples/01-basic-compact.ts",
    "example:multi": "tsx examples/02-multi-compact.ts",
    "example:agent-loop": "tsx examples/03-agent-loop-sim.ts",
    "example:real-api": "tsx examples/04-real-api-compact.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@vitest/coverage-v8": "^3.0.0",
    "dependency-cruiser": "^16.0.0",
    "eslint": "^9.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "files": [
    "dist"
  ],
  "license": "MIT"
}
